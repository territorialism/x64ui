# x64ui 0.0.2-d

the full `main.luau` for this release is the 5.5.1 lineage with version string `0.0.2-d`.

local artifact path used during build: `main_0.0.2-d.luau` (~141kb, ~5085 lines).

replace tip `main.luau` with that file so the library is discoverable on the repo (no paste hosts).
